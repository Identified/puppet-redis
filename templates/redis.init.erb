description "Redis Server"

<% if @autostart %>
start on [filesystem and net-device-up IFACE=lo]
<% end %>
stop on runlevel [!2345]

# Start Redis
exec /usr/bin/redis-server /etc/redis/redis_<%= @port %>.conf
